// Code your solutions in this file
  
const names = ["teddy bear", "drone", "doll"];

function wrapGifts(names, surprise) {
    const message =[]
    for (let i = 0; i < gift.length; i++) {
        
        message.push(`wrapped ${gifts[i]} and added a bow!`)
    }
    return message;
}

let counter = -1
function countDown(counter){
  while (counter >= 0){
    console.log(counter--)
  }
  
}